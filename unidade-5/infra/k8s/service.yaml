apiVersion: v1
kind: Service
metadata:
  name: hospital-api
  namespace: hospital
  labels:
    app: hospital-api
spec:
  type: NodePort
  selector:
    app: hospital-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30080
